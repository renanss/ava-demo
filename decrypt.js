const fs = require("fs"); const { ethers } = require("ethers"); async function decryptKeyFile() { const keyFile = fs.readFileSync("/Users/renansoares/.eigenlayer/operator_keys/test.ecdsa.key.json", "utf8"); const wallet = await ethers.Wallet.fromEncryptedJson(keyFile, "TestPassword123!@#"); console.log("Private Key:", wallet.privateKey); console.log("Address:", wallet.address); } decryptKeyFile().catch(console.error);
